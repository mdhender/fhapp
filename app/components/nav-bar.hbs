<nav>
  <LinkTo @route="index">Home</LinkTo>
  <LinkTo @route="inbox">Inbox</LinkTo>
  {{#if session.isAuthenticated}}
    <button type="button" {{on "click" this.logout}}>Logout</button>
  {{/if}}
</nav>